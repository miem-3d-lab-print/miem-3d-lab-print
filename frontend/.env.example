# Backend API base URL. Include the /api prefix.
# Development (direct to backend):
VITE_API_URL=http://localhost:8080/api
# Production (behind nginx reverse proxy on the same origin):
# VITE_API_URL=/api

# Multipart field used by POST /applications.
VITE_FILES_FIELD_NAME=files[]
